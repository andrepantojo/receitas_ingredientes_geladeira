//rafce
import React from 'react'

const ListaReceita = (props) => {
  return (
    <div>aqui o resultado</div>
    // props.pics.map((pic) => (
    //     <Imagem 
    //         pic={pic.src.small}
    //         alt={pic.alt}
    //     />
    // ))
  )
}

export default ListaReceita